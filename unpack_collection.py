fruit = ["orange", "apple", "banana"]
x,y,z=fruit
print(x)
print(y)
print(z)
# Explanation:
# In this example, we have unpacked a collection into multiple variables.
# The number of variables must match the number of values in the collection.
# The values are unpacked in order.